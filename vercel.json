{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": ".",
  "functions": {
    "api": {
      "runtime": "vercel-node@2.0.0"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api"
    }
  ]
}
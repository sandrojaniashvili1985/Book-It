{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": ".",
  "functions": {
    "api": {
      "runtime": "vercel-node@2.0.0",
      "target": "npm start"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api"
    }
  ]
}